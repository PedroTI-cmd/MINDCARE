# Dependências de desenvolvimento e testes
# Execute: pip install -r requirements-dev.txt

-r requirements.txt

pytest>=8.0,<9.0
pytest-flask>=1.3,<2.0
pytest-cov>=5.0,<6.0
ruff>=0.4,<1.0          # linter + formatter (substitui flake8 + isort + black)
